spring.application.name=helapure-backend
server.port=8080

# ---- MySQL connection ----
# Make sure the DB name here matches your actual database!
spring.datasource.url=jdbc:mysql://localhost:3306/helapure_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=dini123
# driver not required but harmless
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---- JPA ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.main.allow-bean-definition-overriding=true

# Don?t auto-run schema/data scripts unless you intend to
spring.sql.init.mode=never
